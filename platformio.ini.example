[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; Use the default partition table or customize as needed. The uploadfs target
; will upload the contents of the `data/` directory to the device filesystem.

; To upload the data folder (SPIFFS/LittleFS):
; platformio run --target uploadfs
